<% provide :title, @user.name %>
<div class="row">
	<aside class="span4">
		<section>
			<h1 class="align-left">
				<%= gravatar_for @user, size: 80 %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>